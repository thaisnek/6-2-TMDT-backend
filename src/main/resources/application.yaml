server:
  port: 8080
  servlet:
    context-path: /httmdt

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/tmdt"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true